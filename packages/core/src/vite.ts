// TODO v0.2: Vite plugin for automatic regeneration + HMR
export function themeUnifyPlugin(_options?: { config?: string }) {
  return {
    name: "theme-unify",
    // Placeholder — full implementation in v0.2
  };
}
